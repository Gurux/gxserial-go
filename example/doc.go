/*
Package main contains a command-line example for gxserial.

The example shows how to:
  - configure a serial connection from command-line flags
  - register media callbacks (trace, state, error, receive)
  - send a message
  - read a synchronous reply using EOP-based receive parameters
*/
package main
